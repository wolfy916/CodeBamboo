import { DocsMain } from '@/components/docs/DocsMain';
import React from 'react';

interface Props {}

const Docs = ({}: Props) => {
  
  return (
  <div className="relative w-full h-full overflow-y-none">
    <DocsMain />
  </div>
  );
};

export default Docs;
